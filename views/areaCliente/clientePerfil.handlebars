<section>
    <div class="secao-home-cliente">
        <div class="row">
            <div class="col-2">
                {{> clienteAcessos}}
            </div>
            <div class="col-6 secao-home-cliente-row-div-central">
                <form>
                    <div class="form-row">
                        <input hidden type="text" value="{{id}}"></inpu>
                        <div class="col-md-12 mb-12">
                            <label>Nome completo</label>
                            <input type="text" name="nome" value="{{pessoa.nome}}" class="form-control" placeholder="nome" required>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label>cpf</label>
                            <input type="text" class="form-control" value="{{pessoa.cpf}}" placeholder="sobrenome" required>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label>Data de Nascimento</label>
                            <input type="text" class="form-control" value="{{pessoa.dataDeNascimento}}" placeholder="sobrenome" required>
                        </div>
                        <div class="form-group col-md-6">
                            <label>Email</label>
                            <input type="email" name="email" id="email" value="{{pessoa.email}}"  class="form-control" placeholder="Email" required>
                        </div>
                        <div class="form-group col-md-6">
                            <label>Senha</label>
                            <input type="password" class="form-control" value="{{pessoa.senha}}" placeholder="Senha" required>
                        </div>

                        <div class="col-md-12 mb-12">
                            <label>Endereço
                                <input name="cep" class="form-control" type="text" id="cep" placeholder="CEP" value="{{pessoa.endereco}}"
                                    size="100%" onblur="pesquisacep(this.value);" required /></label>
                        </div>
                        
                    </div>
                </form>
            </div>
        </div>
    </div>
</section>